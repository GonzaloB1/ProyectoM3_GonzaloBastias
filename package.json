{
  "name": "pi3-chat",
  "version": "1.0.0",
  "description": "SPA para chatear con personajes ficticios usando Gemini AI",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}